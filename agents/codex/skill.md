# Codex Integration Notes

Use the `webmcp` skill for WebMCP Process-to-Component implementation.

Treat templates as instructional scaffolds.

Inspect the target application before editing.

Preserve existing architecture, dependencies, and conventions.

Use one authoritative schema and one shared process handler.

Derive WebMCP JSON Schema from the authoritative schema.

Use document.modelContext.

Use AbortSignal for registration lifecycle and propagate execution AbortSignal.

Do not add demo data.

Do not add fallback behavior.

Do not modify unrelated files.
